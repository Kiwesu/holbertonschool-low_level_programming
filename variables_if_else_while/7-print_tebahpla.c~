#include <studio.h>

/**
 * main - Prints the lowercase alphabet in reverse, followed by a new line
 * Return: Always 0
 */
int main(void)
{
  char ch = 'z';

  while (ch >= 'a')
  {
    putchar(ch);
    char--;
  }

  putchar('\n');
    return(0);
}
